# ============================================================
# Cloudflare Python Workers 专用依赖清单
# 策略：优先纯 Python 包，避免 C 扩展（因为 Workers 运行在 PyEmscripten）
# 注意：以下包在部署时会被 pywrangler 自动打包
# ============================================================

# ---------- 核心 Web 框架（纯 Python，官方兼容） ----------
flask==3.0.0
flask-cors==4.0.0
werkzeug>=3.0.1

# ---------- WSGI → ASGI 桥（把 Flask 转成 Workers 可识别的格式） ----------
asgiref>=3.7.2

# ---------- 认证（纯 Python） ----------
PyJWT==2.8.0
python-dotenv==1.0.0

# ---------- Excel / CSV 导入导出（纯 Python） ----------
openpyxl==3.1.2

# ---------- 数据库驱动（pg8000 是纯 Python PostgreSQL 驱动；psycopg2 是 C 扩展禁用） ----------
pg8000>=1.30.4

# ---------- 验证码绘图（Pillow 依赖 C 扩展但 Pyodide 提供预编译 wheel） ----------
# 如果部署时 Pillow 编译失败，可以在 Cloudflare 变量里设 DISABLE_CAPTCHA=1 关闭验证码
# 然后在下面这行前面加 # 注释掉
Pillow>=10.3.0
